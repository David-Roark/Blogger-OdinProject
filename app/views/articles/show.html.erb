<h2><%= @article.title %></h2>
<p><%= @article.body %></p> <br><br>
<%= link_to 'Edit', edit_article_path(@article) %>
<%= link_to 'Delete', article_path(@article), method: :delete, data: {confirm: "Are you sure to delete #{@article.title}?"} %> <br>
<%= link_to 'Back to Articles', articles_path %>
